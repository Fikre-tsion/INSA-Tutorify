## 2025-07-05 - Enhanced Interactive Feedback
**Learning:** Adding immediate visual feedback (loading states) for asynchronous operations like login and contact form submission significantly reduces perceived latency and prevents duplicate submissions.
**Action:** Always implement `disabled` states and text indicators on buttons during `fetch` requests in this application.
